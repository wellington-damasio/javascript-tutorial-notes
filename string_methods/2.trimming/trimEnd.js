// The trimEnd() method returns a new string from the original string with the 
//ending whitespace characters stripped. The trimEnd() method doesn’t change the 
//original string.
let str = '    JavaScript    '
let trimmedStr = str.trimEnd()
let fullyTrimmed = str.trim()
console.log({str, trimmedStr, fullyTrimmed)

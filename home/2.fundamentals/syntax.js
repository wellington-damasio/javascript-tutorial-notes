// WHITESPACES
// JavaScript has the following whitespaces:
//   1. Carriage return
//   2. Space
//   3. New line
//   4. tab
// JavaScript engine ignores whitespaces, so it is used to make code more reabable and maintainable

// Without whitespace
let formatted = false; if(!formatted){console.log('This code is not formatted')}

// With whitespaces
formatted = true

if(formatted) {
  console.log('This code is formatted')
}

// Blocks
// A block is a sequence of zero or more simple statements
if(window.localStorage) {
  console.log('The local storage is supported')
}

// IDENTIFIERS
// An identifier is a name you choose for variables, parameters, functions, classes, etc. An identifier name starts with a letter (a-z, or A-Z), an underscore(_), or a dollar sign ($) and is followed by a sequence of characters including (a-z, A-Z), numbers (0-9), underscores (_), and dollar signs ($).

// KEYWORDS & RESERVED WORDS
break, case, catch
continue, debugger, default
else, export, extends
function, if, import
new, return, super
throw, try, null
void, while, with
class, delete, finally
in, switch, typeof
yield, const, do
for, instaceof, this,
var

// FUTURE RESERVED WORDS
enum, implements, let
protected, private, public
await, interface, package,
public
